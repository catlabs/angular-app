import { TableHostDirective } from './table-host.directive';

describe('TableHostDirective', () => {
  it('should create an instance', () => {
    const directive = new TableHostDirective();
    expect(directive).toBeTruthy();
  });
});
